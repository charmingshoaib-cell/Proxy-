# Walkthrough: Age Verification Processor Setup

I've moved the age verification simulation script from your temporary directory to a dedicated workspace and configured it to run as a local server.

## Changes Made

### Workspace Configuration
*   **New Directory**: `C:\Users\shoaib\.gemini\antigravity\scratch\age-verify-processor`
*   **Environment**: Initialized a Node.js project and installed `hono`, `tsx`, and `@hono/node-server`.

### Code Enhancements
*   Moved the script from a `Temp` folder to the workspace.
*   Added a `serve()` call to the bottom of `index.ts` so it can be executed as a standalone Node.js server.

## Verification Results

### Server Status
The server is currently **RUNNING** in the background.
*   **Endpoint**: `http://localhost:3000`
*   **Health Check**: Verified using `Invoke-RestMethod`.

> [!TIP]
> **How to Test Verification**:
> You can now test the simulation by sending a POST request to `/verify`:
> ```powershell
> Invoke-RestMethod -Method Post -Uri http://localhost:3000/verify -Body '{"qrUrl": "YOUR_QR_URL_HERE"}' -ContentType "application/json"
> ```

## Troubleshooting
If you need to restart the server, you can run:
```powershell
npx tsx index.ts
```
